AskGogo Feature 2 — Bidirectional Google Calendar (move / reschedule / cancel)
==============================================================================
NO OAuth migration (scope already auth/calendar read-write). NO DB migration.
Confirms before every mutation (confidence-aware: 1 match -> "yes"; many -> pick a number).

APPLY (from repo root  C:\Users\gover\gogo-memory-os):
  1) Replace/add the two files:
       lib\services\google-calendar.ts        (full file: adds list/update/delete)
       lib\bot\handlers\calendar-mutations.ts (NEW)
  2) Run the wiring patch:
       copy patch_calendar_mutations.py to repo root, then:
       python patch_calendar_mutations.py     (expect "+ ..." x2, "DONE.")
  3) npx tsc --noEmit        (same 5 pre-existing errors, none new)
  4) git add -A ; git commit -m "feat: bidirectional calendar (move/reschedule/cancel)" ; git push ; npx vercel --prod
  5) Remove-Item patch_calendar_mutations.py ; git add -A ; git commit -m "chore: remove patch" ; git push

TEST ON A THROWAWAY EVENT FIRST (Google API is untestable in build env):
  - Create a dummy event in Google Calendar
  - "cancel the <dummy>"   -> should ask "Cancel X? reply yes" -> yes -> deleted
  - "move my 3pm to 4pm"   -> "Move X 3pm->4pm? yes" -> moved, duration kept
  - ambiguous title        -> numbered list -> reply a number
