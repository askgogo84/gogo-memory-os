# P0 acceptance boundary

This hotfix is complete only when the exact BookMyShow forward from the production report is routed as a booking/event, the preview image is never inspected as a document, and the full regression/build gate is green. Merge only after the preview is READY.
